start{
    integer a is 6;
    integer b is 4;
    integer c is 5;
    if(a>b){
        if(a>c){
            show(a);
        }
        else{
            show(c);
        }
    }else{
        if(b>c){
            show(b);
        }else{
            show(c);
        }
    }
}